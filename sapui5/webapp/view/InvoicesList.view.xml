<mvc:View
    controllerName="logalilaboratorio.sapui5.controller.InvoicesList"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
>
    <List
        id="invoicesList"
        headerText="{i18n>InvoiceListTitle}"
        items="{ path: 'northwind>/Invoices', 
                 sorter: { path: 'ShipName',
                           group: true }
                }"
    >
        <headerToolbar>
            <Toolbar id="ToolBarList">
                <Title id="ToolBarListTitle" text="{i18n>InvoiceListTitle}"></Title>
                <ToolbarSpacer id="ToolBarListTS"/>
                <SearchField
                    id="searchProduct"
                    width="50%"
                    search=".onFilterInvoices"/>
            </Toolbar>
        </headerToolbar>
        <items>
            <ObjectListItem
                id="invoiceItem"
                title="{= ${northwind>Quantity} + ' x ' + ${northwind>ProductName} }"
                number="{ parts : [ {path : 'northwind>ExtendedPrice'},
                                    {path : 'currency>/usd'}],
                          type: 'sap.ui.model.type.Currency',
                          formatOptions: {
                                showMeasure : false,
                                maxFractionDigits: 2,
                                preserveDecimals: false
                          }}"
                numberUnit="{currency>/usd}"
                numberState="{= ${northwind>ExtendedPrice} > 50 ? 'Success' : 'Error' }"
            >
            </ObjectListItem>
        </items>
    </List>
</mvc:View>
